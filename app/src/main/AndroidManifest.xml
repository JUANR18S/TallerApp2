<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TallerApp2">

        <!-- Activities internas -->
        <activity
            android:name=".CalculadoraActivity"
            android:exported="false" />
        <activity
            android:name=".PromedioActivity"
            android:exported="false" />
        <activity
            android:name=".ConversorActivity"
            android:exported="false" />
        <activity
            android:name=".ProgramasActivity"
            android:exported="false" />
        <activity
            android:name=".SecondActivity"
            android:exported="false" />

        <!-- Activity de arranque -->
        <activity
            android:name=".PruebaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
